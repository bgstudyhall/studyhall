data/
__pycache__/

# Google Classroom - Keep credentials private (but NOT the cache)
google_cookies.pkl
classroom_config.json
# Note: announcements_cache.json is NOT ignored - it should be committed and deployed
